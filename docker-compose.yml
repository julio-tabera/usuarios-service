version: '3.3'

services:

  redis:
    container_name: users-redis
    image: redis:alpine
    ports:
      - "127.0.0.1:6309:6309"
    networks:
      - users-network

  nginx:
    image: users/nginx:1.21
    container_name: users-nginx
    build:
      context: .
      dockerfile: ./docker-config/nginx/Dockerfile
    ports:
      - "127.0.0.1:8097:80"
    volumes:
      - ./src/usuarios:/var/www/html
      - ./docker-config/symfony/.env:/var/www/html/.env
      - ./docker-config/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - users-network
    depends_on:
      - php-fpm

  php-fpm:
    image: users/php:8.2-fpm
    container_name: users-php
    build:
      context: .
      dockerfile: ./docker-config/php/Dockerfile
    volumes:
      - ./src/usuarios:/var/www/html
      - ./docker-config/php/db_install.sh:/var/www/html/db_install.sh
      - ./docker-config/php/timezone.ini:/usr/local/etc/php/conf.d/timezone.ini
    networks:
      - users-network
    depends_on:
      - postgres
    environment:
      DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PW}@postgres:5432/${POSTGRES_DB}"

  postgres:
    container_name: users-postgres
    image: postgres:latest
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PW}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "127.0.0.1:5460:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - users-network
    restart: always

  pgadmin:
    container_name: users-pgadmin
    image: dpage/pgadmin4:latest
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_MAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PW}
    ports:
      - "127.0.0.1:5079:80"
    networks:
      - users-network
    restart: always

networks:
  users-network:

volumes:
  db_data:
